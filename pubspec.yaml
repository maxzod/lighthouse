name: lighthouse
description: lighthouse will watch your project files changes and restart the app automaticity
version: 0.0.2
publish_to: "none"
homepage: https://github.com/maxzod/lighthouse
repository: https://github.com/maxzod/lighthouse
issue_tracker: https://github.com/maxzod/lighthouse/issues
environment:
  sdk: ">=2.12.0 <3.0.0"
  # flutter: ">=1.17.0"

dependencies:
  path: ^1.8.0
  # watch files system
  watcher: ^1.0.0
  # to log in the terminal with colors
  ansicolor: ^2.0.1
  args: ^2.3.0
  cli_dialog: ^0.5.0
  recase: ^4.0.0
  # nations:
  #   git:
  #     url: git@github.com:maxzod/nations.git

dev_dependencies:
  # lint
  lints: ^1.0.1

executables:
  lh: lighthouse
  lighthouse: lighthouse
